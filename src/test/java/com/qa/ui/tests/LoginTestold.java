package com.qa.ui.tests;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.devtools.v134.browser.Browser;

import com.qa.utilities.BrowserUtility;

public class LoginTestold {
	
	public static void main(String[] args) {
		/*
		WebDriver driver= new ChromeDriver();		//Launch a browser window!! Browser session is created!!
		
		BrowserUtility browserutility= new BrowserUtility(driver);
		browserutility.goToWebSite("http://www.automationpractice.pl/index.php");
		browserutility.maximizeWindow();
		
		By signInLinkLocator= By.xpath("//a[contains(text(),'Sign in')]");
		browserutility.clickOn(signInLinkLocator);
		
		By emailTextboxLocator= By.id("email");
		browserutility.enterText(emailTextboxLocator, "automationframework@gmail.com");
		
		By passwordTextboxLocator= By.id("passwd");
		browserutility.enterText(passwordTextboxLocator, "tester1234");
		
		By signInButtonLocator= By.id("SubmitLogin");
		browserutility.clickOn(signInButtonLocator);
		*/
	}

}
